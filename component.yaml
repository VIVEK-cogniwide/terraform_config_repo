apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: k8s-pipeline-component
  description: Cogniassist AKS cluster
  annotations:
    backstage.io/kubernetes-id: aks-pe-poc
    backstage.io/kubernetes-cluster: minikube-local
    backstage.io/kubernetes-namespace: default
spec:
  type: service
  lifecycle: experimental
  owner: user:VIVEK-cogniwide
  # kubernetes:
  #   cluster: minikube-local
  #   namespace: default
   
