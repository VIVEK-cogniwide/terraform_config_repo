# .github/workflows/hello.yml
name: 👋 Simple Log Message

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: 🖨️ Print Hello Message
        run: echo "👋 Hello from GitHub Actions! "
