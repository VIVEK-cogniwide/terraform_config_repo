apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: k8s-pipeline-component
  description: A Kubernetes component associated with the Jenkins pipeline
  annotations:
    backstage.io/kubernetes-id: minikube-local
    backstage.io/kubernetes-cluster: minikube-local
    backstage.io/kubernetes-namespace: default
spec:
  type: service
  lifecycle: experimental
  owner: user:VIVEK-cogniwide
  # kubernetes:
  #   cluster: minikube-local
  #   namespace: default
   
